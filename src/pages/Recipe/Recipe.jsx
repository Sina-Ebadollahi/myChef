// styles
import './Recipe.css'
// hooks
import { useParams } from 'react-router'

export default function Recipe() {
    // getting the changable id parameter
    const {id} = useParams();
    return (
        <div>
            
        </div>
    )
}
